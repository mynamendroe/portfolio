I will create the `/work` and `/contact` pages by refactoring the shared layout logic into a reusable component.

1.  **Create `components/PageWrapper.tsx`**:
    *   Extract the layout logic from `app/page.tsx` (background, `Header`, `Menu` state, `AnimatePresence` toggle).
    *   This ensures consistent navigation and transitions across all pages.

2.  **Refactor `app/page.tsx`**:
    *   Update the Home page to use `<PageWrapper>` instead of duplicating the layout code.

3.  **Create `/work` Page**:
    *   Create `app/work/page.tsx` using `<PageWrapper>`.
    *   Create `components/WorkContent.tsx` with a placeholder project gallery/list to display on this page.

4.  **Create `/contact` Page**:
    *   Create `app/contact/page.tsx` using `<PageWrapper>`.
    *   Create `components/ContactContent.tsx` with contact information (email, social links) and a simple layout.

This approach ensures modularity and keeps the codebase dry, consistent with the existing design system.